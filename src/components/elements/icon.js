//fontAwsome
import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { pors } from "@fortawesome/free-solid-svg-icons";

const fsIcon = ({ pors }) => {
  return <FontAwesomeIcon icon={pors} />;
};

export default fsIcon;
